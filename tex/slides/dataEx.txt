class Data {
  enum DataLocation { Gpu, Cpu };

  // Pointer to GPU memory
  std::unique_ptr<double> dx_
  // Local reference (RAM)
  Eigen::VectorXd x_;
  // DataLocation::Gpu or DataLocation::Cpu
  DataLocation location_;
};
